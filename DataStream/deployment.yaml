apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-datastream
spec:
  selector:
    matchLabels:
      run: my-datastream
  replicas: 1
  template:
    metadata:
      labels:
        run: my-datastream
    spec:      
      containers:
      - name: my-datastream
        image: insanelyfreak/datastream
        ports:
        - containerPort: 8080
apiVersion: v1
kind: Service
metadata:
  name: my-datastream
spec:
  selector:
    app: my-datastream
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
